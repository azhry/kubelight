{
  "identifier": "default",
  "description": "Default capability for KubeLight",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "store:default"
  ]
}
